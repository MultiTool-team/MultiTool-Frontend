// store the Route values in enum type
enum Route {
  Weather = 'Weather',
  Cinema = 'Cinema',
  Blog = 'Blog',
  Friends = 'Friends',
  Shop = 'Shop',
}

// Used in Header
export const ROUTES: Route[] = [
  Route.Weather,
  Route.Cinema,
  Route.Blog,
  Route.Friends,
  Route.Shop,
];
