import { setLocation } from './locationSlice';
import { setWeatherData, setForecastData } from './weatherSlice';
import { themeSlice } from './themeSlice';

export { setLocation, setWeatherData, setForecastData, themeSlice };
